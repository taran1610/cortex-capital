"""
Cortex Capital — modular trading kernel (Phase 1).

This package is the **testable, event-ready** core: market data, strategies,
risk, execution, storage, and a minimal backtest loop. It sits alongside the
**agentic** stack in `graph/` (LangGraph swarm) and integrations in `cortex_tools/`.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
