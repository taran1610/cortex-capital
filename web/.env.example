# URL of the Cortex Python FastAPI (see python/api/app.py).
# Local dev: leave unset (defaults to http://127.0.0.1:8800).
# Vercel: set to your deployed Python service (must allow server-to-server calls).
CORTEX_PYTHON_API_URL=http://127.0.0.1:8800
